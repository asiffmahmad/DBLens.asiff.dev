import { ComingSoon } from "@/components/coming-soon";

export default function RollbackPage() {
  return (
    <ComingSoon 
      title="Rollback Insights" 
      description="Heuristic engine to analyze schema diffs and generate safe rollback SQL previews."
    />
  );
}
