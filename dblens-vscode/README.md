# DBLens Companion

The companion extension for DBLens. 
This extension allows you to spawn the DBLens server directly from your VS Code editor.

## Features
- **Start Local Server**: Spawns `npm run dev` and opens the dashboard in your default browser.

## Requirements
- Node.js 18+
- The `DBLens.asiff.dev` workspace opened in VS Code.
